# =============================================================================
# Eryndor MMO - nginx Configuration
# =============================================================================
# This file should be placed at /etc/nginx/sites-available/eryndor
# Then symlinked to /etc/nginx/sites-enabled/eryndor
#
# Prerequisites:
# - SSL certificates from Let's Encrypt (see DEPLOYMENT.md)
# - nginx with WebSocket support
#
# Usage:
#   sudo ln -s /etc/nginx/sites-available/eryndor /etc/nginx/sites-enabled/
#   sudo nginx -t
#   sudo systemctl reload nginx
# =============================================================================

# Rate limiting zones for DDoS protection
limit_req_zone $binary_remote_addr zone=websocket_limit:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=general_limit:10m rate=30r/s;

# WebSocket proxy for WASM clients
upstream eryndor_websocket {
    server 127.0.0.1:5003;
    keepalive 32;
}

# HTTP -> HTTPS redirect
server {
    listen 80;
    listen [::]:80;
    server_name eryndor-online.com www.eryndor-online.com;

    # Let's Encrypt ACME challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect all other traffic to HTTPS
    location / {
        return 301 https://$server_name$request_uri;
    }
}

# Main HTTPS server
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name eryndor-online.com www.eryndor-online.com;

    # SSL configuration (Let's Encrypt)
    ssl_certificate /etc/letsencrypt/live/eryndor-online.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/eryndor-online.com/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/eryndor-online.com/chain.pem;

    # SSL security settings
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    ssl_stapling on;
    ssl_stapling_verify on;

    # Security headers
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # WebSocket endpoint for game clients
    location /ws {
        # Rate limiting
        limit_req zone=websocket_limit burst=20 nodelay;

        # WebSocket proxy settings
        proxy_pass http://eryndor_websocket;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket timeout settings (important for long-lived connections)
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
        proxy_connect_timeout 60s;

        # Disable buffering for WebSocket
        proxy_buffering off;

        # Security: prevent host header injection
        proxy_set_header Host $http_host;
    }

    # Health check endpoint (optional)
    location /health {
        access_log off;
        return 200 "OK\n";
        add_header Content-Type text/plain;
    }

    # Block common attack patterns
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    # Default - return 404 for unknown paths
    location / {
        return 404 "Not Found\n";
    }
}

# =============================================================================
# Optional: Admin dashboard (if you build one later)
# =============================================================================
# Uncomment and configure if you add a web-based admin dashboard
#
# server {
#     listen 443 ssl http2;
#     server_name admin.eryndor-online.com;
#
#     # SSL configuration (same as above)
#     ssl_certificate /etc/letsencrypt/live/admin.eryndor-online.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/admin.eryndor-online.com/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
